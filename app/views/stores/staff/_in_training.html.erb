<div class="row justify-content-center mb-2">
  <h3 class="text-center">Scheduled For Training</h3>
  <% if @students.size > 0 %>
    <% @students.each do |student| %>
      <div class="col-6 col-md-3 col-lg-2">
        <% if current_user.position.name.in? (["General Manager", "Supervisor", "Operations Manager", "Director"]) %>
          <%= link_to student.full_name, student, class: "table-link" %>
        <% else %>
          <%= student.full_name %>
        <% end %>
      </div>
    <% end %>
  <% else %>
    <h4 class="text-center">No Training Scheduled 😢</h4>
  <% end %>
  <hr class="mt-3" >
</div>