<%= form_with(model: equipment_area) do |form| %>
  <% if equipment_area.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(equipment_area.errors.count, "error") %> prohibited this equipment_area from being saved:</h2>

      <ul>
        <% equipment_area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :organization_id, style: "display: block" %>
    <%= form.text_field :organization_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
